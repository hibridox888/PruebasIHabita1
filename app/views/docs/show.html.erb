<!-El codigo generado en este apartado sirve para visualizar informacion en la base de datos
Se usa la tabla Doc de la  base de datos definida aca como @docs y con esto se genera una lista con los datos separados por un |.
->
<h1> Documentos hasta la fecha </h1><!- esto aparece escrito en tamaño H1 ->

<ul>
    <% @docs.each do |doc| %><!- ingresa a los datos de la tabla Doc agregadas en la variable @docs y le asigna cada uno de estos a la variable doc ->
        <li> <!- genera la lista y muestra los datos de la variable doc ->
        <%= doc.id %> | 
        <%= doc.empresa %> |
        <%= doc.tipo %> |
        <%= doc.rut %> |
        <%= doc.numero %>
        </li>
    <% end %>
</ul>

<%= button_to "Volver", user_session_path %><!- codigo para regresar a la pantalla inicial ->
<%= button_to "Salir", destroy_user_session_path, method: :delete %><!- codigo para salir de la sesión ->
